"""Phase 6 — Dashboard: a minimal FastAPI control panel (mobile PWA) for editing
stage durations and triggering Morning/Night generation. Talks to the rest of the
engine only through config.py's Pydantic models and publish.rebuild_progression —
no Spotify/generator logic lives here.
"""
